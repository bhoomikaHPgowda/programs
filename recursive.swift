let power(base : Double,power : Double) -> Double
 {   
       if power == 0
      {
           return 1.0
       }
       else
      {
       var result : Double
       result = pow(base,power)
       return(result)
       }  
   }
   print(power(base : 5,power : 2))
    
    
